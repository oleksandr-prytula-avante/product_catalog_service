root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/app ."
bin = "tmp/app"
include_ext = ["go"]
exclude_dir = ["tmp", "vendor", ".git"]
delay = 1000

[log]
time = true

[misc]
clean_on_exit = true